{
  "name": "dedalion-device-world",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Installable Dedalion life-world runtime for kiosk and WebView devices.",
  "scripts": {
    "start": "node server.mjs",
    "check": "node --check app.js && node --check device-bridge.js && node --check device-client.js && node --check server.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
